// Single source of truth for assignable admin-panel pages. Mirrored on the
// frontend in src/constants/pages.js (labels only need to match there).
const PAGE_KEYS = [
  'dashboard',
  'billing',
  'products',
  'categories',
  'pending',
  'customers',
  'reports',
  'settings',
];

module.exports = { PAGE_KEYS };
